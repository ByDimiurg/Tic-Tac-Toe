X always goes first

We have 3 game modes:
  player VS player  === checkBoxX = off && checkBoxO = off
  bot VS player     === checkBoxX = on && checkBoxO = off
  player VS bot     === checkBoxX = off && checkBoxO = on